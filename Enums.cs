namespace Clear
{
    public enum ImageSizePreference
    {
        None,
        Width,
        Height
    }

    public enum Sharers
    {
        Facebook,
        Twitter,
        Pinterest,
        Google
    }
}